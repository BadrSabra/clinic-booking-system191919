
## ⚡ التركيب السريع

### الخيار 1: استضافة محلية
1. انسخ جميع الملفات إلى مجلد
2. افتح `index.html` في متصفحك
3. النظام جاهز للاستخدام

### الخيار 2: استضافة على الإنترنت
1. ارفع الملفات إلى أي استضافة ويب
2. افتح رابط الموقع
3. النظام يعمل فوراً

## 🎯 كيفية الاستخدام

### للمرضى:
1. افتح `booking.html`
2. املأ البيانات الشخصية
3. اختر الطبيب والتاريخ والوقت
4. اضغط تأكيد الحجز
5. ستتلقى تأكيداً عبر واتساب

### للطبيب/الإدارة:
1. افتح `dashboard.html`
2. استخدم بيانات الدخول الافتراضية:
   - اسم المستخدم: `admin`
   - كلمة المرور: `clinic123`
3. قم بتعديل الإعدادات من `settings.html`

## ⚙️ التخصيص

### تعديل بيانات العيادة:
1. افتح `settings.html`
2. اذهب إلى قسم "معلومات العيادة"
3. عدل البيانات حسب حاجتك
4. اضغط "حفظ جميع الإعدادات"

### إضافة أطباء جدد:
1. افتح `settings.html`
2. اذهب إلى قسم "قائمة الأطباء"
3. أضف بيانات الطبيب الجديد
4. اضغط "إضافة الطبيب"

### تعديل أوقات العمل:
1. افتح `settings.html`
2. اذهب إلى قسم "أوقات العمل"
3. عدل أوقات كل يوم
4. حدد الأيام النشطة

## 📊 إدارة البيانات

### البيانات المخزنة:
- **الحجوزات**: في `localStorage` باسم `clinic_bookings`
- **الإعدادات**: في `localStorage` باسم `clinic_settings`
- **المرضى**: في `localStorage` باسم `clinic_patients`

### نسخ احتياطي:
```javascript
// تصدير البيانات
const backup = {
    bookings: JSON.parse(localStorage.getItem('clinic_bookings')),
    settings: JSON.parse(localStorage.getItem('clinic_settings')),
    patients: JSON.parse(localStorage.getItem('clinic_patients'))
};

// حفظ كملف JSON
const dataStr = JSON.stringify(backup, null, 2);
